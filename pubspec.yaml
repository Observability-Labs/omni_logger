name: omni_logger
description: "Omni Logger: Production-Ready Logging for Flutter"
version: 1.0.4
homepage: 'https://github.com/Observability-Labs/omni_logger'
repository: https://github.com/Observability-Labs/omni_logger

environment:
  sdk: ^3.8.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ^2.7.0
  path: ^1.9.1
  device_info_plus: ">=10.1.0 <14.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true